(Esto es una importacion que agrego el profe) // #Agregar arriba en el anterior archivo en la parte superior
from servicio.catalogo_peliculas import CatalogoPeliculas as cp 


if opcion == 1:
	nombre_pelicula = input('Digite el nombre de la pelicula:')
	pelicula = Pelicula(nombre pelicula)
	cp.agregar_peliculas(pelicula)
elif opcion == 2:
	cp.listar_peliculas()
elif opcion == 3:
	cp.eliminar_peliculas()
except Exception as e:
	print(f' ocurrio un error de tipo:{e}')
	opcion = None
else:
	print('Salimos del programa...')
#Fernando Silva	
